import React from 'react';
import CartItem from '../Components/CartItem/CartItem'; // Ensure the path is correct

const Cart = () => {
  return (
    <div className="cart-page">
      <CartItem />
    </div>
  );
};

export default Cart;
